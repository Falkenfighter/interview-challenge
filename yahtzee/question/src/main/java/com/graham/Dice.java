package com.graham;

/**
 * This is a very simple Dice object. The constructor takes and should store the number of sides
 * the dice have (A standard yahtzee die has 6 sides).
 */
public class Dice {

    // Step 1) Store the number of sides in an instance variable
    public Dice(final Integer numOfSides) {
    }

    // Step 2) "roll" a random Integer for each of the die
    // Step 3) return an int array containing the rolled values
    public int[] roll(final Integer numOfDice) {
        return null;
    }
}
